apiVersion: v1
kind: Service
metadata:
  name: {{ include "preview-environments-example.fullname" . }}
  labels:
    {{- include "preview-environments-example.labels" . | nindent 4 }}
  namespace: {{ .Release.Namespace }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "preview-environments-example.selectorLabels" . | nindent 4 }}
